import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Map = props => {
   return (
      <View>
         <Text>Our Map</Text>
      </View>
   );
};
const styles = StyleSheet.create({});
export default Map;